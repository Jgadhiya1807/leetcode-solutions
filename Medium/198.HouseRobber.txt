class Solution:
    def rob(self, nums: List[int]) -> int:
        prev = nums[0]
        prev2 =0
        lent= len(nums)
        for i in range(1,lent):
            take = nums[i]
            if(i>1): 
                take+=prev2
            prev2=prev
            prev = max(take,prev)
        return prev